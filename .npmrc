# Allow build scripts for all dependencies
# Required for Prisma, esbuild, sharp, and other native dependencies
# In pnpm v10+, build scripts are blocked by default for security
dangerously-allow-all-builds=true
